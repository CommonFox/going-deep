# ESPN draft sheet — 2026

**League:** ESPN, 10-team, **1QB** (no superflex), full PPR, 20 roster spots (QB / RB×2 / WR×2 /
TE / FLEX / K / P / DST / 7 bench / 2 IR). This league also starts a **punter** — the one position
no external projection source in this warehouse prices; `punter_projections` is the only model that
does.

**Seat: not yet known.** ESPN's `draftSettings.orderType` is `DRAFT_START`, which randomizes the
pick order when the draft room opens — the `pickOrder` already sitting in the league record is
provisional and `src.draft.live_espn` refuses to resolve a seat from it before then (confirmed live
against the real league on 2026-09-08: it refuses with exactly that message). **THE SHEET's
pick-by-pick table and the opening plan (Convo A) can't be written until the order is drawn** — see
the TODO at the bottom. Everything below this point doesn't need a seat.

All numbers are from the warehouse build (days-old, not recomputed live): `draft_board`
(points over replacement = **POR**), `archetype_outcomes` (surplus by career-stage archetype). Both
filtered to `league_key = 'espn'` — this league's own scoring and replacement level, not
Sleeper's superflex numbers.

---

## Convo B — archetypes & tiebreakers, this league's own numbers

### The two signals this league's data actually clears

`archetype_outcomes`, 8 seasons, **espn** (crossed against `draft_value`'s ADP-relative surplus, not
raw draft-day surplus — see `player_archetypes.py`'s docstring for why that adjustment matters).
Same two cells the Sleeper sheet found are reliable here too, with this league's own numbers:

| Archetype | Edge vs ADP | |
|---|---|---|
| **RB / Proven Prime** (young/prime **and** a prior elite finish) | **+11.9 pts** (t = 1.72) | target |
| **WR / Unproven Veteran** (journeyman, never elite, 27+/7yr+) | **−3.8 pts** (t = −4.0) | fade |

Everything else the table reports — QB Proven Prime, RB Proven Veteran, TE Proven Prime, WR Proven
Prime, and the rest — is gated off (`is_reliable = False`): the surplus mean is close enough to zero,
or the standard error wide enough, that it isn't distinguishable from noise. Notably QB shows
**nothing reliable at all** in this league, unlike a superflex league where QB is where the real
signal concentrates — 1QB doesn't reward or punish a QB archetype the way owning two QB slots does.

- **Proven Prime RBs in range** (checked against `player_archetypes` directly, not copied from the
  Sleeper sheet — several names differ, see below): Gibbs, Bijan, Achane, Chase Brown, Jeanty,
  Breece Hall, Kyren Williams, Javonte Williams, Bucky Irving. **Not** Jonathan Taylor, CMC, Saquon
  or James Cook — all four are `Proven Veteran`, a different archetype this table doesn't flag as
  reliable in this league. The archetype classification is a fact about the *player*, but which
  archetype a player is in *can* differ from what an older writeup assumed — Saquon and CMC in
  particular have aged into Proven Veteran since.
- **Unproven Veteran WRs to fade:** the same "aged, never-elite journeyman" shape — check this
  league's specific waiver/bench options against `player_archetypes` closer to the draft, since the
  exact names available this late will depend on who's gone by each pick.

### Color: QBs still don't bust, but this league doesn't pay extra for two of them

Every QB archetype's `injured_rate`/`busted_rate` pattern (get hurt or deliver, essentially never
"bust" the way a bench RB does) holds regardless of league — that's a fact about the position, not
the scoring format. What's different here is the *why-it-matters*: in the superflex league two
starting QB slots make position 2 of that pair nearly as valuable as position 1, which is what
forces the QB run in rounds 2-3. In a 1QB league there's only one slot to fill, so the QB
POR curve (Josh Allen 83, next tier Hurts 39, Lamar 37 — nothing like the superflex board's Josh
Allen at 140) never creates that same urgency. `draft_strategy_summary`'s own superflex
counterfactual (see `README.md`'s `draft_strategy.py` entry) says this directly: point the model at
this league's real (non-superflex) settings and it stops recommending an early quarterback.

---

## Convo C — the endgame, adjusted for a 20-round, punter-starting league

### K / P / DST

`draft/hold.py`'s rule: reserve `RESERVE_ROUNDS_PER_SLOT` (3) rounds per late slot the league
starts, counted back from the end. This league starts **three** such slots (K, P, DST — Sleeper's
has no punter, hence only K + DST there), so reserve = 9 and
`from_round = rounds - reserve + 1 = 20 - 9 + 1 = 12`. The live tool holds all three off the default
board until **round 12**, whichever three picks that turns out to be once the seat is known. Typing
`k`, `p` or `dst` overrides it at any point.

That number isn't a guess — it's this league's own draft history. Replaying all four prior ESPN
seasons (2022-2025), K/P/DST don't stagger apart by position at all; pooled together they cluster
into one specialist run, with 10% of all real picks at those positions already made by round 12 and
a quarter by round 13. Two more obvious-looking anchors were checked and rejected: external market
ADP (which says DST goes ~round 10 and K doesn't start until ~round 16 — this league doesn't draft
like the wider market) and this league's own points-over-replacement (P actually outranks K here,
which argued for taking P first — real behavior says otherwise, K goes first on average across all
four seasons despite being the weaker position by value).

**Kickers, ranked (this league's POR):**

| Player | Team | Bye | POR | ADP |
|---|---|---|---|---|
| Brandon Aubrey | DAL | 14 | 14.9 | 130.2 |
| Cameron Dicker | LAC | 7 | 13.1 | 153.7 |
| Ka'imi Fairbairn | HOU | 8 | 9.9 | 160.4 |
| Jason Myers | SEA | 11 | 8.9 | 155.7 |
| Wil Lutz | DEN | 10 | 1.5 | 201.3 |

Steep drop after the top four — Myers to Lutz is most of the position's spread in one step.

**Punters, ranked (this league's POR — no external site prices these, so no ADP column exists):**

| Player | Team | Bye | POR |
|---|---|---|---|
| Tommy Townsend | TEN | 9 | 23.1 |
| Bradley Pinion | MIA | 6 | 22.9 |
| Sam Martin | CAR | 5 | 17.7 |
| AJ Cole | LV | 13 | 13.3 |
| Cameron Johnston | PIT | 9 | 12.3 |

Notice the top punter (23.1 POR) clears the top kicker (14.9) by a wide margin — real value says
punter is the stronger position here, even though real draft-room behavior (above) takes kickers
first anyway.

Unlike the Sleeper sheet's K/DST cutoff, this one does *not* line up with where the board's own
value fully runs out — see below. Rounds 12-14 still carry real, if thin, positive POR; the round-12
release is tracking this league's actual draft-room behavior (above), not a claim that nothing
better is left. That's a real, deliberate tradeoff: taking a specialist at round 12 costs a little
more than waiting to round 15, in exchange for drafting the way this room actually drafts.

### The value window, read off the board's own POR curve

Grouping `draft_board` by ADP-implied round (10-team snake, so round *r* ≈ ADP 10(r−1)+1 to 10r) and
taking the best POR available in each round:

| Round band | Best POR still available |
|---|---|
| 1–11 | 23–171 (real, startable value every round) |
| 12–14 (K/P/DST unlock here) | 11–21 (thin, but still positive) |
| 15–17 | 0–13 (flat, noisy — small samples, a few injured-discount RBs) |
| 18–22 | flat at roughly 0, occasionally negative |

**The value window is rounds 1–14, not 15+.** By round 15 the board stops offering anything
reliably above replacement, which is a few rounds later than the Sleeper sheet's read of its own
board (rounds 6–9) — expected, since a 10-team league has a shallower player pool draining slower
per round than a 14-team one. K/P/DST now unlock two rounds *before* that value window ends (round
12, not round 15+), which is the tradeoff described above.

### "Super-low ADP players we can differentiate"

Same caveat as the Sleeper sheet: `draft_value`/`player_archetypes` read the wrong ADP for whichever
league doesn't match their join (see the bug note at the bottom of `sleeper-2026.md` — same root
cause, `adp_consensus`'s `(player, format)` join). Re-check `draft_board`'s own blended view (which
joins correctly) closer to the draft for this league's specific market-vs-model gaps, rather than
trusting `draft_value`/`player_archetypes` numbers directly.

---

## TODO — fill in once ESPN draws the draft order

- [ ] **Convo A — the opening plan.** Read `draft_plans`/`draft_availability` filtered to
  `league_key = 'espn'` and this seat's `draft_slot`, the same way `sleeper-2026.md`'s Convo A was
  built. (`draft_plans` already has rows for every seat 1-10 — nothing to rebuild, just query it
  once `pickOrder` is final.)
- [ ] **THE SHEET.** The pick-by-pick table: this seat's actual overall pick numbers (snake, 10
  teams, 20 rounds — `espn_seat.resolve_seat` will report them), what's likely there each time, and
  the target composition read off Convo A.
- [x] ~~Re-verify `src.draft.live_espn` against a real `mDraftDetail` payload~~ — done 2026-09-08,
  not by waiting for the room to open but by replaying this league's actual completed 2025 draft
  (170 real picks) through `espn_seat`/`espn_picks`/`screen` end to end: seat, roster and the whole
  rendered board all came out correct, and every real pick did carry `teamId` as `_is_mine` assumes.
  What's still unverified is the payload's *shape while a draft is actually in progress* — the 2025
  replay was a completed draft fetched whole, not picks arriving one at a time from a live room.
